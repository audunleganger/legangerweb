# Caddy env
ROOT_HOST=localhost
MATMENY_HOST=matmeny
MATMENY_ALT_HOSTS=mat.localhost www.mat.localhost www.matmeny.localhost
HEXUS_HOST=hexus.localhost

# Docker Compose env
CADDY_HTTP_PORT=80
CADDY_HTTPS_PORT=443
FRONTEND_PORT=3000
MATMENY_FRONTEND_PORT=3000
MATMENY_BACKEND_PORT=8000
MATMENY_DB_PORT=5432
HEXUS_PORT=3000

# Matmeny DB env
MATMENY_DB_USER=user
MATMENY_DB_PASSWORD=password
MATMENY_DB_NAME=matmeny